---
import VueComponent from "./Example10.vue";
import Header from "./Header.astro";

const { title, description } = Astro.props;
---

<>
  <Header title={title} description={description} />
  <VueComponent client:only="vue" />
</>
